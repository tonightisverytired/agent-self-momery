# dnamemory 系统设计与系统架构设计

> 版本：v1.0（2026-08-03）｜适用版本：0.3.0

## 1. 总体架构

分层设计，核心为纯 Python 库，服务与 CLI 均为薄壳：

```text
┌────────────────────────────────────────────────────┐
│ 接入层：CLI / FastAPI 侧车 / FastMCP / 第三方调用   │
├────────────────────────────────────────────────────┤
│ 门面层：MemorySystem（公共 API v1，零配置）         │
├────────────────────────────────────────────────────┤
│ 领域层：retrieval（多路+RRF+重排）/ governance      │
│         （生命周期/冲突/反射/消解）/ extract（LLM）  │
├────────────────────────────────────────────────────┤
│ 存储层：SQLiteStore（事务/幂等/审计/向量索引）      │
│ 适配层：Embedder / Reranker / Extractor 协议        │
└────────────────────────────────────────────────────┘
```

## 2. 模块职责

| 模块 | 职责 |
|---|---|
| `dnamemory.models` | 数据模型与 `MemoryConfig`（全部可配置项，校验在构造期） |
| `dnamemory.store` | SQLite 存储、事务、幂等、审计、生命周期状态机、sqlite-vec 索引 |
| `dnamemory.retrieval` | 时间/图谱/稠密/稀疏四路召回、RRF 融合、过滤链、并列规则 |
| `dnamemory.governance` | 衰减/遗忘/恢复、冲突治理、合规删除级联、反射压缩、主题簇 |
| `dnamemory.memory` | `MemorySystem` 门面：写入、召回、治理入口、实体消解、事实版本链 |
| `dnamemory.metrics` | Recall/Precision/MRR/MAP/NDCG 评测 |
| `dnamemory.extract` | DeepSeek 抽取参考实现 + Pydantic 逐条校验 + 确定性兜底 |
| `dnamemory.embeddings` | `Embedder` 协议 + bge-m3（稠密+稀疏）参考实现 |
| `dnamemory.rerank` | `Reranker` 协议 + bge-reranker-v2-m3 参考实现 |
| `dnamemory.backends` | 可插拔检索后端：`TimeBackend`/`GraphBackend` 协议、Postgres 时间链、Neo4j 空间链（默认 None 时零派发） |
| `dnamemory.cli` | 命令行（9 个子命令，零配置） |
| `dnamemory.pg_store` | PostgresStore 设计占位（未接入） |
| `app/dnamemory_server.py` | FastAPI 侧车（Bearer 鉴权、错误码映射） |
| `app/dnamemory_mcp.py` | FastMCP 服务（4 个工具，stdio/streamable_http） |

## 3. 数据模型与存储

### 3.1 表结构

| 表 | 用途 |
|---|---|
| `nodes` | 事件与实体统一异构节点（type/kind/name/ts/value_score/access_label/lifecycle/life/decay_rate） |
| `edges` | 带类型关联边（14 类 rel、weight/confidence、双时态 valid_at/invalid_at、lifecycle） |
| `facts` | 实体事实（key/value/source/confidence/双时态/superseded_by/tombstoned） |
| `versions` | 节点版本链 |
| `tombstones` | 删除/合并墓碑（唯一删除入口） |
| `audit_log` | 全部写/改/删/合并/收敛审计 |
| `node_vectors` | 稠密+稀疏向量（dense 强校验，sparse best-effort） |
| `vector_index_meta` | ANN 索引元数据（按 model+dim 分区） |

### 3.2 生命周期状态机

```text
active ──衰减──▶ archived ──遗忘──▶ tombstoned ──合规──▶ deleted
   │ ▲             │ ▲                 │ ▲
   └─恢复──────────┘ └──恢复───────────┘ └──── 终态（唯一出口）
```

- 合法迁移白名单在 `store.LEGAL_LIFECYCLE`，非法迁移抛 `E001`；
- `deleted` 不可恢复（`restore` 抛 `E004`）；每次非幂等迁移写审计；
- 合规删除（`forget(force=True)`）在同一事务内级联：节点 deleted、关联 fact tombstoned、关联 edge tombstoned、写墓碑与审计。

## 4. 检索管线

### 4.1 路径与融合

| 模式 | 路径 |
|---|---|
| `time` | 仅时间路 |
| `graph` | 仅图谱路 |
| `semantic` | 稠密路（无 embedder 时词面代理） |
| `dual` | 时间 + 图谱 |
| `triple` | 时间 + 图谱 + 稠密语义（默认） |
| `quad` | 时间 + 图谱 + 稠密语义 + 稀疏路 |

融合：每路独立排序，按 `score/(rrf_k + rank)` 累加（RRF，rrf_k=60）；并列时按 路径顺序（time<graph<semantic<sparse）→ node_id 升序。

### 4.2 阈值与打分

- 稠密路：余弦相似度 ≥ `dense_min_sim=0.55`，打分 `sim×(0.5+0.5×value_score)`；
- 稀疏路：bge-m3 lexical_weights L2 归一化余弦 ≥ `sparse_min_sim=0.25`，同型打分；
- 时间路：`1/(1+diff_days)`，容差 `tol_days=2`；
- 图谱路：实体种子 BFS（max_hops=2），`(1/(hop+1))×weight×confidence×value_score`；
- 重排：注入 reranker 后对候选池前 `rerank_top_n=20` 重排；异常/分数异常一律回退原序。

### 4.3 过滤与安全

`RecallFilters`：node_types / kinds / time_range / include_archived / access_labels；`deleted` 永不返回，`sensitive` 默认隐藏。

## 5. 治理机制

| 机制 | 行为 |
|---|---|
| 受控衰减 | 每日按 decay_rate 扣 life，≤0 归档；protected 零衰减 |
| 价值加固 | `access()` 按 value_score 增加 life |
| 冲突治理 | 同 (node,key) 活跃事实按 source_rank→confidence→valid_at 排序，败者 `superseded_by` 胜者；同源高置信进入 confirm |
| 反射压缩 | 月度摘要节点（value_score=源均值×0.8，access_label 继承最高敏感级）+ summarizes 边 + 主题簇（事件 part_of 簇、簇 part_of 摘要） |
| 实体消解 | `resolve_entities()` 同名合并（不同 access_label 不合并）、边/事实重挂、自边清除、重复节点墓碑；写入端点模糊匹配阈值 0.88 |
| 事实版本链 | `fact_history(entity, key)` 返回按 valid_at 排序的完整事实链 |

## 6. 向量索引与降级链

- 索引：sqlite-vec `vec0` 余弦虚拟表，按 `(model, dim)` 独立分区，存归一化稠密向量；
- 同步：`set_node_vectors` 提交后增量 DELETE+INSERT；meta 不匹配自动全量重建；
- 检索：`_dense_path` 优先 `search_vectors`，失败回退 numpy 暴力；numpy 缺失回退双路——任何一层失败不阻断召回，top-k 集合与 numpy 一致。

## 7. 嵌入形态

### 7.0 可插拔检索后端（0.4.0）

- `MemorySystem(time_backend=None, graph_backend=None)`：缺省时检索走内置 SQLite 直连路径，不经过协议派发；
- 注入 `PostgresTimeBackend` / `Neo4jGraphBackend` 后，对应路径改为调用后端，RRF/过滤/重排逻辑不变；
- 后端查询只读，写入仍走 SQLite；数据由 `sync_memory_to_postgres` / `sync_memory_to_neo4j` 预置；
- 选型见 [后端选型.md](./后端选型.md)。

```text
进程内库（默认）──▶ FastAPI 侧车 ──▶ FastMCP ──▶ Postgres（设计占位）
```

- 进程内：`MemorySystem(path="user_memory.db")`，零服务零网络；
- FastAPI：`GET /health`、`POST /recall|write|forget`，Bearer token 必填，错误码映射（E004/E006→404，E009→503，E011→502，其余→400/409/500）；
- MCP：`dnamemory_recall / dnamemory_write_memory / dnamemory_forget / dnamemory_resolve_conflicts`；stdio 本机无鉴权，streamable_http 强制 Bearer；
- 单进程单库为默认模型，文档明确多进程共享限制。

## 8. 安全与错误码

- token 用 `secrets.compare_digest` 常量时间比较；token 由调用层/环境注入（库内不读环境变量）；
- 路由显式枚举，无路径拼接；SQL 全部参数化（索引表名仅由 int/白名单字符构造）；
- 敏感记忆：sensitive 默认隐藏、deleted 永不返回、合规删除物理保留但全路径不可见；
- 错误码体系：E000 通用 / E001 校验 / E002 数值域 / E003 双时态 / E004 不可恢复 / E005 权限 / E006 不存在 / E008 冲突 / E009 存储 / E010 缺依赖 / E011 向量。

## 9. 关键设计决策（ADR）

| 决策 | 结论 |
|---|---|
| 关系建模 | 废弃"碱基对"，采用事件/实体异质节点 + 14 类带类型关联边 |
| 召回架构 | 多路并行 + RRF，行业 2026 年共识（Mem0/Hindsight/Zep 同向） |
| 语义路 | bge-m3（1024 维稠密 + 词级稀疏），中文友好、本地缓存 |
| 存储 | SQLite + WAL + 单写锁 + 独立读连接；sqlite-vec 仅作加速投影 |
| 治理 | 双时态 + 来源可信级 + 墓碑审计 + 受控遗忘 |
| 配置 | 全部在代码调用层（构造级/方法参数/回调注入），零配置文件 |
| 服务化 | 同一内核薄壳，进程内默认；模型权重不打包 |
