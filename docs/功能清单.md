# dnamemory 功能清单

> 版本：v1.1（2026-09-15）｜适用版本：0.5.0

## 1. 核心库功能

| 类别 | 功能 | 入口 | 状态 |
|---|---|---|---|
| 写入 | 事件/实体/关联边/事实结构化写入 | `MemorySystem.add_*` | ✅ 已实现 |
| 写入 | Belief/Intent/Evidence 结构化写入（同事务+幂等+审计） | `store.insert_belief/intent/evidence` + `_write_candidates` | ✅ 已实现 |
| 写入 | 证据绑定（evidence 候选先落表，批内记忆 evidence_ids 回填） | `_write_candidates` | ✅ 已实现 |
| 状态 | 确定性查询意图路由（当前/历史/时间线/变化，无 LLM） | `resolve.query_router` | ✅ 已实现 |
| 状态 | FactResolver 确定性裁决（过滤/时间有效集/superseded_by/来源可信级/冲突态） | `resolve.FactResolver` | ✅ 已实现 |
| 状态 | BeliefResolver 观点演化时间线（不覆盖历史） | `resolve.BeliefResolver` | ✅ 已实现 |
| 状态 | IntentResolver 意图状态（status+时间有效性） | `resolve.IntentResolver` | ✅ 已实现 |
| 状态 | ConflictResolver 四类冲突分类（Hard/Temporal/Source/Soft） | `resolve.ConflictResolver` | ✅ 已实现 |
| 状态 | MemoryStateResolver 维度编排（局部失败不阻断） | `resolve.MemoryStateResolver` | ✅ 已实现 |
| 时间链 | 稳定时间排序 + 链拼接（版本链/显式 links/邻接） | `temporal.TemporalChainBuilder` | ✅ 已实现 |
| 一致性 | 跨维度一致性检查（互斥 fact 进冲突并带解释） | `coherence.CrossDimensionCoherence` | ✅ 已实现 |
| 上下文 | 候选四类扩展 + 证据校验（inferred 不得伪装为事实）+ ContextBuilder | `context.expand/validate/ContextBuilder` | ✅ 已实现 |
| 上下文 | recall_context 管线（召回→扩展→解析→链→校验→一致性→结构化上下文） | `MemorySystem.recall_context` | ✅ 已实现 |
| 查询 | memory_history / timeline / explain | `MemorySystem.memory_history/timeline/explain` | ✅ 已实现 |
| 存储 | beliefs/intents/evidence/memory_links 四张新表 + 旧库自动迁移（幂等补列） | `SQLiteStore` | ✅ 已实现 |
| 评分 | MemoryScore 分项评分（仅 recall_context 管线，recall 零变化） | `models.MemoryScore` | ✅ 已实现 |
| 写入 | LLM 文本拆解（Pydantic 逐条校验、幂等） | `write_text / write_many` + `DeepSeekExtractor` | ✅ 已实现 |
| 召回 | 时间路 | `recall(mode="time")` | ✅ 已实现 |
| 召回 | 图谱路（14 类关联边、BFS 多跳） | `recall(mode="graph")` | ✅ 已实现 |
| 召回 | 稠密语义路（bge-m3，阈值 0.55） | `recall(mode="semantic/triple")` | ✅ 已实现 |
| 召回 | 稀疏第四路（bge-m3 lexical weights，阈值 0.25） | `recall(mode="quad")` | ✅ 已实现 |
| 召回 | RRF 融合 + 过滤链 + 并列规则 | `recall(k, filters, mode)` | ✅ 已实现 |
| 召回 | 可选重排（rerank_top_n=20，失败回退） | `MemorySystem(reranker=...)` | ✅ 已实现 |
| 治理 | 生命周期状态机（非法迁移拒绝） | `store.update_lifecycle` | ✅ 已实现 |
| 治理 | 受控衰减/价值加固/保护区 | `step_day / access` | ✅ 已实现 |
| 治理 | 普通遗忘（墓碑）/ 合规删除（deleted+级联） | `forget(force=...)` | ✅ 已实现 |
| 治理 | 恢复（deleted 拒绝） | `restore` | ✅ 已实现 |
| 治理 | 冲突治理（来源可信级/superseded_by/confirm） | `resolve_conflicts / confirm` | ✅ 已实现 |
| 治理 | 事实版本链 | `fact_history(entity, key)` | ✅ 已实现 |
| 治理 | 访问控制（sensitive 隐藏） | `access_label` + `RecallFilters` | ✅ 已实现 |
| 治理 | 反射压缩（月度摘要/主题簇/敏感级继承） | `reflect_monthly` | ✅ 已实现 |
| 治理 | 实体消解（同名合并/端点模糊匹配/自边清除） | `resolve_entities` + 写入管线 | ✅ 已实现 |
| 存储 | SQLite 事务/幂等/审计/墓碑/版本 | `SQLiteStore` | ✅ 已实现 |
| 存储 | sqlite-vec 余弦 ANN（自动重建/增量/降级） | `store.rebuild_vector_index / search_vectors` | ✅ 已实现 |
| 存储 | Postgres DDL（写路径未实现，查询侧用 PostgresTimeBackend） | `tools/pg_migrate.py` | ⏸ 设计占位 |
| 后端 | `TimeBackend`/`GraphBackend` 协议（默认零派发） | `MemorySystem(time_backend=..., graph_backend=...)` | ✅ 已实现 |
| 后端 | Postgres 时间链后端（对拍一致） | `PostgresTimeBackend` + `sync_memory_to_postgres` | ✅ 已实现 |
| 后端 | Neo4j 空间链后端（top-k 一致） | `Neo4jGraphBackend` + `sync_memory_to_neo4j` | ✅ 已实现 |

## 2. 接入形态

| 形态 | 功能 | 入口 | 状态 |
|---|---|---|---|
| Python API | 全量能力 | `from dnamemory import MemorySystem` | ✅ 已实现 |
| CLI | add-event / add-entity / add-edge / add-fact / recall / forget / reflect / demo / eval / history / timeline / context / explain | `dnamemory <cmd>` | ✅ 已实现 |
| FastAPI 侧车 | health / recall / write / forget / context / timeline / memory/{id}/history / memory/{id}/explain + Bearer 鉴权 | `py -m app.dnamemory_server` | ✅ 已实现 |
| FastMCP | recall / write_memory / forget / resolve_conflicts / context / timeline / explain | `py -m app.dnamemory_mcp` | ✅ 已实现 |

## 3. 评测与门禁

| 工具 | 内容 | 状态 |
|---|---|---|
| `pytest`（全量用例） | 单元/集成/服务/索引/规模优化/状态层/时间链/一致性回归 | ✅ 实测全部通过 |
| `tools/gate_check.py` | 双门禁：快照召回（dual Recall≥0.95、MAP≥0.90、triple≥dual）+ 状态正确性（§25 Test 01-05） | ✅ 实测通过（召回 1.000/1.000，状态全过） |
| `tools/eval_longterm.py` | 中文五类能力评测（自包含语料，默认内存库；temporal/multi_hop/update/abstain/paraphrase） | ✅ 实测 bge-m3 triple/quad 均 1.000 |
| `tools/eval_state.py` | 状态正确性五类评测（temporal_chain/belief_change/fact_conflict/evidence_grounding/cross_dimension） | ✅ 实测五类均 1.000 |
| `tools/pg_migrate.py` | Postgres DDL 生成/执行 | ✅ 已实现 |

## 4. 验证结果摘要（2026-08-03）

| 项 | 结果 |
|---|---|
| pytest 全量 | 59 passed |
| 门禁 dual/triple | Recall 0.993→1.000，MAP 0.978→0.996 |
| 中文能力评测（bge-m3，triple 与 quad） | 五类均 1.000 |
| 安装 | `pip install -e .` 成功，`dnamemory --version` = 0.3.0 |
| CLI 全链路 | demo → recall → forget 均正常 |
| 待外部执行 | reranker 同义改写对比（需下载约 2GB 模型） |

## 5. 明确不包含

- Postgres 写路径完整实现（仅 DDL：tools/pg_migrate.py）；
- LongMemEval 官方仓库适配器；
- 多进程共享同一库文件；
- 模型权重的离线打包分发。
